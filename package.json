{
  "name": "cinedo",
  "version": "1.0.0",
  "source": "index.html",
  "browserslist": "> 0.5%, last 2 versions, not dead",
  "description": "",
  "scripts": {
    "sass": "sass --no-source-map --style=compressed src/scss:src/css",
    "netlify-dev": "netlify dev",
    "build": "concurrently \"npm run sass\" \"npm run netlify-dev\"",
    "netlify-build": "netlify build",
    "build2": "concurrently \"npm run sass\" \"npm run netlify-build\""
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "concurrently": "^8.2.2",
    "dotenv": "^16.4.4",
    "netlify-cli": "^17.16.2",
    "netlify-lambda": "^2.0.16",
    "parcel": "^2.11.0",
    "sass": "^1.71.0",
    "sass-loader": "^14.1.0"
  }
}
